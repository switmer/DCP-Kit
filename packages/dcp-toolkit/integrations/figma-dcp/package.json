{
  "name": "figma-dcp-plugin",
  "version": "0.1.0",
  "description": "DCP Design System integration for Figma - sync tokens and validate designs",
  "main": "code.js",
  "scripts": {
    "build": "esbuild src/code.ts --bundle --outfile=code.js --target=es2017",
    "watch": "esbuild src/code.ts --bundle --outfile=code.js --target=es2017 --watch",
    "dev": "npm run watch",
    "package": "npm run build && npm run create-bundle",
    "create-bundle": "mkdir -p dist && cp manifest.json ui.html code.js README.md dist/ && echo '✅ Figma plugin packaged in ./dist/' && echo '📁 Ready for Figma Community or development testing'"
  },
  "keywords": [
    "figma",
    "plugin",
    "design-system",
    "tokens",
    "validation",
    "dcp"
  ],
  "author": "DCP Platform",
  "license": "MIT",
  "devDependencies": {
    "@figma/plugin-typings": "^1.88.0",
    "@types/node": "^20.0.0",
    "esbuild": "^0.19.0",
    "typescript": "^5.0.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/stevewitmer/dcp-transformer.git",
    "directory": "integrations/figma-dcp"
  }
}