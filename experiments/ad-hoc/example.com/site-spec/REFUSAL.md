---
source_url: https://example.com
hostname: example.com
extracted_at: 2026-04-20T23:35:15.452Z
extraction_mode: automatic
extractor: Get-Site-Styles (semantic-bindings v0.1.0)
synthesizer: build-design-md.mjs (DCP live-site pipeline, refusal path)
substrate_quality_score: 0.07
refusal_threshold: 0.55
role_coverage_floor: 0.50
refuse_reason: role coverage 0.07 below hard floor 0.5
status: refused
---

# DESIGN.md — example.com *(refusal)*

> **Reconstruction-grade narrative was NOT generated for this site.** Substrate quality score `0.07` is below the refusal threshold `0.55`. The authoring surface exposed does not carry enough evidence to reconstruct a design system with the fidelity a full DESIGN.md claims. Below you'll find the raw extraction — use it as material, not as a spec.

## Why this refusal

Each dimension contributes to the substrate score. Weak dimensions explain the refusal:

| Dimension | Value | Weight | Detail |
|---|---|---|---|
| `roleCoverage` | 0.07 | 0.45 | 1 / 14 DCP roles bound — BELOW HARD FLOOR (0.5) |
| `colorCandidateDepth` | 0.10 | 0.20 | 1 usable color candidates |
| `fontSizeCoverage` | 0.13 | 0.15 | 1 font-size tokens |
| `spacingCoverage` | 0.00 | 0.10 | 0 spacing tokens (scale+section) |
| `responsiveSignal` | 0.00 | 0.10 | no clamp() or @media rules observed |

Prose is sticky — a reader skimming a full 9-section narrative does not reliably downweight it based on confidence labels alone. The honest behavior at this substrate level is to emit the raw extraction and say so, rather than write smoother-than-warranted prose.

## Raw extraction — use as material, not as spec

### Color role bindings (what the auto-mapper did find)

- **border.default** — `#eee`  
  *Confidence:* 0.63 (low) · *Reason:* direct: border → border.default

**13 of the DCP contract's roles were unmapped** on this site: `bg.default`, `bg.muted`, `bg.elevated`, `text.primary`, `text.muted`, `text.onAccent`, `border.muted`, `accent.primary`, `accent.secondary`, `intent.danger`, `intent.warning`, `intent.success`, `intent.info`. That is the bulk of the refusal reason — with most semantic slots empty, narrative synthesis would be mostly invention.

### Typography (raw only)

```
1.5em
```

### Spacing (raw, classified)

- layout: `15vh`

## What would change this refusal

- **Bind more roles.** The extractor left 13 DCP roles unmapped. Adding site-crawl breadth (subpages beyond the homepage), running with `--use-browser` for JS-rendered sites, or improving role-detection heuristics are the three levers.
- **Broaden substrate.** Typography, spacing, and responsive signals score below threshold. A site that declares no `@media` and minimal `clamp()` inherently produces less extractable substrate than one that does.
- **Lower the threshold.** `REFUSAL_THRESHOLD = 0.55` in `build-design-md.mjs`. If you want narrative anyway on low-substrate sites, the system is willing — just at the explicit cost of readers reading smoother prose than evidence warrants.

_Generated by DCP live-site pipeline (refusal path) · synthesizer `experiments/lib/build-design-md.mjs` · 2026-04-20T23:35:15.452Z_
