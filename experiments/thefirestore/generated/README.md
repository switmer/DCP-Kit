# Generated transpile output — known-imperfect, committed as evidence

Same situation as `../../bungee-pro/generated/`. See that directory's README for the full list of known transpile defects (literal `\n` escapes, duplicate `children` destructures, flattened variant Tailwind classes). Defects are inherited from `src/commands/transpile.js` and are universal across registries, not specific to this experiment.

See `../FINDINGS.md` for strategic framing.
